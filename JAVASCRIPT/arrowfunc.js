/* Problem Statement
Array: scores = [85, 92, 78]

Use forEach() with arrow function (score) => {} to:

Print each score prefixed with "Score: "

Expected: 3 lines total

*/
let scores = [85, 92, 78];

scores.forEach((score) => {
    console.log("Score:", score);
});